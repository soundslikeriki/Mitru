# Mitru v0.9.7-beta 最終総合テストチェックリスト

## 今回の自動確認結果

- [x] `npm run build` が成功した
- [x] Vite buildでchunk size警告が出ていない
- [x] 工事項目マスタ・材料マスタでReactの重複key警告が出ないことを確認した
- [x] `/dashboard`, 積算タブ, 請求書タブ, `/payments`, `/reports`, `/masters` のスモーク確認が成功した
- [x] `npm run test:beta-load` で80案件・400積算行の集計負荷テストが成功した
- [x] サイドバー全14メニューのクリック遷移が成功した
- [x] Dashboardの欠損データ復旧ガードを確認した
- [x] Dashboard/Reportsのstore購読を浅い比較に変更した
- [x] PDF生成系の通常画面での常時ロードを避けるよう調整した
- [x] localStorageサイズ監視警告を追加した
- [x] `npm run tauri:build -- --bundles app` が成功し、macOS `.app` を生成した
- [ ] `npm run tauri:build` の `.dmg` 作成は、このCodex環境では `hdiutil: create failed - Device not configured` により未完了。リリース用Mac環境で再確認する
- [x] 主要14サイドバーメニューのブラウザスモークテストが成功した
  - `/dashboard`
  - `/reports`
  - `/projects`
  - `/estimates`
  - `/invoices`
  - `/payments`
  - `/billing`
  - `/purchases`
  - `/deliveries`
  - `/orders`
  - `/customers`
  - `/masters`
  - `/settings`
  - `/help`
- [x] ブラウザスモークテスト範囲で重大なエラー画面が出ていない
- [x] `/dashboard`, `/projects`, `/estimates`, `/invoices`, `/billing`, `/payments`, `/purchases`, `/reports`, `/settings` の再確認が成功した
- [x] `2026-05-13` に `npm run build` を再実行し、成功した
- [x] `2026-05-13` に `npm run test:beta-load` を再実行し、80案件・400積算行の集計負荷テストが8msで完走した
- [x] `/dashboard`, `/projects`, `/masters`, `/estimates`, `/invoices`, `/billing`, `/payments`, `/purchases`, `/reports`, `/settings`, `/help` のブラウザスモーク確認が成功した
- [x] 内装工事マスタの中項目・細目表示がマスタ設定画面でErrorBoundaryなしに表示されることを確認した

## 1. 主要E2Eフロー

- [ ] 案件を作成し、顧客情報・工事期間・入金予定日を保存できる
- [ ] 積算タブで人件費・材料費を追加し、数量・単価・法定福利費率を入力できる
- [ ] 粗利率が30%未満は黄、25%未満は赤で警告表示される
- [ ] 積算から見積書を作成し、見積書一覧と案件内見積書タブに反映される
- [ ] 見積書から請求書を作成し、請求書一覧と案件内請求書タブに反映される
- [ ] 請求締めから一括請求書を作成し、連番とスナップショットが保持される
- [ ] 入金登録で残債が更新され、ダッシュボードの未入金表示に反映される
- [ ] 発注書から仕入・支払登録ができ、支払予定カードに反映される
- [ ] 納品書・注文書を作成し、各一覧とプレビューに反映される
- [ ] 進行管理タブでステータス、次回対応日、工程メモ、担当者メモを保存できる
- [ ] レポートページで月次売上、粗利、原価内訳、ランキングが表示される
- [ ] ダッシュボードに利益、リスク、キャッシュフロー、入金、支払、次回対応が反映される

## 2. 書類・PDF

- [ ] 見積書、請求書、納品書、注文書のプレビューが開く
- [ ] PDF出力で会社ロゴ、社判、顧客情報、会社情報、明細、合計が崩れない
- [ ] 明細が10件を超える場合でも複数ページに自然に分割される
- [ ] 印刷時にサイドバー、編集パネル、不要ボタンが出力されない

## 3. データ復旧・安定性

- [ ] アプリ全体のErrorBoundaryが表示できる構成になっている
- [ ] localStorage破損時に `mitru-local-store-corrupted-backup-*` または `mitru-local-store-error-backup-*` として退避される
- [ ] 退避後、初期状態でアプリを再起動できる
- [ ] リロード後も案件、顧客、積算、書類、設定が保持される

## 4. 大量データ・パフォーマンス

- [x] 80件以上の案件、400件以上の積算行で集計処理が完走する
- [ ] 50件以上の積算行で入力・スクロールが極端に重くならない
- [ ] 案件一覧、顧客一覧、マスタ一覧で検索・フィルターが軽快に動作する
- [ ] 主要ページが遅延読み込みされ、初期表示が極端に遅くならない
- [ ] `npm run build` が成功し、chunk size警告が許容範囲に収まる

## 4.1 大量データ負荷テスト手順

- [ ] 検証前に「アプリ設定 > データ出力」から現データのバックアップを取得する
- [ ] 検証用データとして案件80件以上、積算行400件以上を投入する
- [ ] `/projects`, `/dashboard`, `/reports`, `/payments`, `/purchases` を順に開き、重大な描画エラーが出ないことを確認する
- [ ] 案件詳細の積算タブで数量・単価を編集し、入力遅延が実用範囲であることを確認する
- [ ] 検証後はバックアップから通常データへ戻す

## 5. ベータ公開前確認

- [ ] バージョン表示が `0.9.7-beta` になっている
- [x] CHANGELOG.mdにベータ版の変更内容が記載されている
- [ ] 初回起動オンボーディングでサンプル一式読み込みが推奨表示されている
- [ ] ヘルプページ `/help` から基本操作を確認できる
- [x] READMEまたはリリース案内に「ローカル保存」「ベータ版」「バックアップ推奨」を明記する
- [ ] `docs/cloud-sync-test-checklist.md` に沿ってクラウド同期OFF/ONの確認を行う
