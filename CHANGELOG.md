# Changelog

## [0.9.7-beta] - 2026-05-19

限定ベータ公開候補。ローカルファーストの主要業務フローを維持しながら、Bring Your Own Supabase方式のクラウド同期を実験的機能として追加し、公開前の安全策、警告表示、バックアップ導線を強化しました。

### Added

- Bring Your Own Supabase方式のクラウド同期設定を追加。
- Supabase Project URL / Anon Keyの入力、接続テスト、Supabase認証モーダルを追加。
- 案件、顧客、見積書、請求書、入金記録の段階的な同期処理を追加。
- 同期進捗、最終同期、同期履歴、成功/失敗/未実行の状態表示を追加。
- 初回同期確認ダイアログと、Supabaseプロジェクト作成ガイドへの導線を追加。
- クラウド同期ON/OFFを含むテスト手順を `docs/cloud-sync-test-checklist.md` に追加。
- 設定画面、ヘルプ、READMEの警告文を「クラウド同期は実験的機能です。データ消失の可能性があります。必ずバックアップを取ってからONにしてください。」に統一。
- 実Supabase回帰テスト用の「同期テストモード」を追加。ONの間だけ、同期対象件数、entity別cursor、push/pull結果をコンソールへ詳細出力。
- 競合解決ダイアログに「ローカル全部優先」「クラウド全部優先」と主要項目の差分ヒントを追加。
- 全一覧ページとマスタ設定ページの横スクロール仕様を統一し、13インチ幅でも列を潰さず確認できるように調整。

### Changed

- バージョンを `0.9.7-beta` に更新し、アプリ内表示を `v0.9.7-beta (限定ベータ)` に統一。
- READMEにクラウド同期機能、Bring Your Own Supabase方式、初回同期、セキュリティ注意事項を追記。
- クラウド同期はオフライン優先を維持し、同期OFF時は認証や同期を要求しない設計に整理。
- 同期処理は案件→顧客→見積書→請求書→入金記録の順で実行し、一部失敗しても成功済みの部分とローカルデータを保持する方針に整理。
- アプリ内表示バージョンと `package.json` のバージョン表記を `0.9.7-beta` に統一。
- クラウド同期の安全性を強化。entity別cursor、削除同期のtombstone、競合解決、DB `updated_at` トリガーを前提に、同期漏れと意図しない上書きを抑制。
- `syncMetadata` 未作成または `lastSyncedAt` 未設定の新規レコードは必ずpush対象にするよう、dirty判定を強化。
- クラウド同期設定画面の警告、ステータス、下部ガイドのレイアウトを限定ベータ公開向けに調整。
- クラウド同期処理に軽量な共通ヘルパーを追加し、pull/push/sync処理の重複を削減。
- デスクトップ配布向けにTauriのCSPを最低限設定し、`object-src` / `frame-src` を閉じつつ、BYO Supabase接続に必要な `https:` / `wss:` 通信を許可。
- Tauri Updaterによる自動更新を削除し、更新確認はGitHub Releasesを開く手動更新導線に統一。

### Fixed

- `lastSyncedAt` のentity共通利用による同期漏れリスクを、entity別cursorへ分離して修正。
- ローカル時刻依存の差分判定を避けるため、Supabase側 `updated_at` と `(updated_at, id)` cursorを同期基準に変更。
- ProjectHeader、ReportsPage、OrdersPageなどで発生しやすかったselector由来の無限再レンダリングを予防修正。
- 配布前の作業で生成された不要な空ファイルを削除し、リポジトリ直下を整理。
- Windowsビルド時に不要なUpdater署名処理が走らないよう、Updater設定、権限、Rust/JS依存、ビルドスクリプトの署名キー注入を削除。

### Beta Notes

- クラウド同期は実験的機能です。データ消失の可能性があります。必ずバックアップを取ってからONにしてください。
- 積算明細（`projectItems`）は0.9.7-betaのクラウド同期対象外です。積算明細を含む完全な退避・移行にはJSONバックアップを使用してください。
- Supabase側のテーブル、RLS、権限設定が不完全な場合は同期に失敗します。その場合でもMitruのローカルデータは保持されます。
- 複数端末で同じデータを同時編集する運用は、限定ベータ中は慎重に検証してください。

## [0.9.6] - 2026-05-14

### Added

- 入金消込を強化し、請求書ごとの未入金、一部入金、入金済、過入金、残債を確認しやすくした。
- ダッシュボード、進行管理、入金一覧、レポートを限定ベータ向けに確認しやすく整理。
- localStorageの保存サイズを監視し、5MBに近づいた場合にバックアップ推奨の警告を表示。
- 内装工事マスタを床仕上げ、壁・天井仕上げ、造作・建具、水回り内装、その他内装の中項目で拡充。

### Changed

- バージョンを `0.9.6` に更新。
- ベータ公開前チェックリストを、積算、見積、請求締め、入金、発注、レポートの主要フローに合わせて更新。
- READMEと配布手順のバージョン表記を `0.9.6` に統一。
- READMEに限定ベータ向けの初回起動手順、macOS Gatekeeper、Windows SmartScreen、バックアップ推奨を追記。
- MitruをGoolipのローカル代替として開発中であること、限定ベータ中にフィードバックを募集することを明記。
- DashboardとReportsのstore購読を浅い比較に変更し、集計画面の不要な再描画を軽減。
- PDF生成系の呼び出しを遅延読み込みに寄せ、通常画面表示時の常時ロードを抑制。
- 顧客管理の「すべて / 新規 / 既存」フィルターを復旧し、顧客種別フィルターと並べて表示。
- ダッシュボード上部に「今日のMitru」「Mitruへようこそ」の最小ブランド見出しを復旧。
- `npm run tauri:build:mac` で `.app` と `.dmg` を同時生成する配布向け挙動に変更。

### Fixed

- 工事項目マスタ・材料マスタのID生成をUUID優先に変更し、短時間の大量追加でReactの重複key警告が出る問題を修正。
- 既存localStorage内に重複したマスタIDがある場合、migrationでデータを消さずにIDだけ安全に補正するようにした。
- マスタ作成・更新時のupsertを整理し、同一マスタの重複作成を防止。
- Dashboardで古い保存データや欠損日付を含む請求・入金・発注データがある場合にErrorBoundaryへ落ちる問題を修正。
- サイドバーの背景レイヤーと重なり順を調整し、全メニューのクリック遷移を安定化。
- 工事項目マスタの中項目表示を復旧し、展開行は「中項目 › 細目」のシンプルな表示に整理。
- `FeaturePageTitle` / `PageTitle` の未使用propsを削除し、ページタイトルはグローバルヘッダー側に集約。

### Beta Notes

- このバージョンは限定ベータ公開候補です。実案件投入前に、必ずデータ出力で控えを作成してください。
- 端末内保存を前提にしているため、端末移行、アプリ更新、ブラウザ/アプリデータ削除の前にはバックアップを推奨します。
- コード署名・公証前の配布では、macOS GatekeeperやWindows SmartScreenの警告が出る可能性があります。
- 大量データでの最終確認は、配布前の実機環境でも再確認してください。
- macOS `.dmg` とWindowsインストーラーは、配布対象OSの実機またはCIで最終生成確認を行ってください。

## [0.9.5-beta] - 2026-05-12

### Added

- レポートページを追加し、月次売上、粗利、原価内訳、粗利率分布、高利益案件、注意案件ランキングを確認できるようにした。
- ダッシュボードに今月の業績サマリー、未入金、支払予定、レポート連携のカードを追加。
- 入金管理、請求締め、一括請求、購買・発注・支払管理の簡易版を追加。
- 法定福利費率を設定・計算に反映し、労務費、法定福利費、総原価を確認できるようにした。

### Changed

- バージョンを `0.9.5-beta` に更新。
- 初回オンボーディングのサンプルデータ読み込みを、ベータ検証用の推奨アクションとしてより目立つ表示に調整。
- 見積、請求、納品、注文、入金、発注、レポートの主要フローをベータ公開前チェックリストに整理。
- READMEにベータ版としての注意事項とバックアップ推奨を明記。
- macOS `.app` / `.dmg`、Windows NSIS `.exe` 向けの配布設定とビルドスクリプトを整理。
- 配布ビルド手順を `docs/distribution.md` に追加。
- `src-tauri/icon.svg` からmacOS/Windows用アイコン一式を再生成。

### Fixed

- マスタ更新による過去案件・発行済書類の破壊を防ぐため、書類スナップショットとマイグレーションを強化。
- 請求累計、採番、案件削除時の関連データ削除、ステータス表記ゆれを修正。
- localStorage persist、バックアップ復元、ErrorBoundary、画像データ肥大化への対策を強化。
- ライトモード、タブ、警告、レポート画面、PDF/印刷プレビューの視認性を最終調整。

### Beta Notes

- このバージョンはベータ公開候補です。実案件投入前に、データ出力でバックアップを取得してください。
- 現在の保存先は端末内localStorageです。SQLite永続化は未使用です。

## [0.9.0-beta] - 2026-05-09

### Added

- Phase 1: 階層型積算、見積/実行単価分離、歩掛改善、クイック追加、積算テンプレートを追加。
- Phase 2: 利益予実管理、案件利益ダッシュボード、キャッシュフロー予測、今年の業績予測を追加。
- Phase 3: 積算から見積・請求・納品・注文へつながる書類作成フローと案件進行管理を追加。
- Phase 4: ヘルプページ、コンテキストヘルプ、初回オンボーディング、サンプル案件読み込みを追加。
- アプリ全体のErrorBoundaryを追加し、画面描画エラー時に再読み込み・保存データ退避復旧を選べるようにした。

### Changed

- バージョンを `0.9.0-beta` に更新。
- 初回起動時は、粗利率の違う4件の実務向けサンプル案件を読み込む導線をおすすめ表示に変更。
- 主要ページの遅延読み込みとViteのchunk分割を調整し、初期ロードとビルド警告を改善。
- App.tsx、設定、マスタ、顧客、案件詳細、積算、見積、請求、書類関連をfeatures配下へ段階分割。

### Fixed

- localStorage破損時に即削除せず、破損データを退避してから初期化する復旧処理に変更。
- ライトモードの文字コントラスト、タブ、バッジ、印刷プレビュー、顧客・案件フォームの細かな視認性を調整。
- 見積書・請求書・納品書・注文書のプレビュー/PDFの複数ページ、ロゴ、社判、顧客情報表示の整合性を改善。

### Beta Notes

- Mitruはローカルファーストのベータ版です。実運用前には、重要データをエクスポートして控えを残してください。
- 現時点では端末内のlocalStorageを主な保存先として使用します。SQLite永続化は未使用です。
