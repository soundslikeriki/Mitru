# Mitru environment variables
#
# Mitru is designed to run fully local and offline. No server endpoint is required
# for the default desktop build.

# Set to 1 only when you need debug-friendly frontend source maps/minification.
TAURI_DEBUG=0

# Vite exposes variables prefixed with VITE_ to the renderer.
VITE_APP_NAME=Mitru
